{
  "name": "promedia-news",
  "private": true,
  "version": "1.0.0",
  "description": "news.promedia.report — новинний сайт ПроМедіа з мультиавторською адмінкою",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute promedia-news --local --file=migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute promedia-news --remote --file=migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
